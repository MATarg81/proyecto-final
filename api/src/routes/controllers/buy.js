const { Buy } = require("../../db")

const buyOrder = async(req, res) => {
    const data = req.body
    console.log("Data de buyOrder: ", data)
    try {
        
    } catch (error) {
        console.log(error)
        
    }
}